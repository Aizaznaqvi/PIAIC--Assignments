const firstName: string = "Umar";
const secondName:string = "Tanveer"
const firstLowerCase:string= firstName.toLowerCase();
const secondLowerCase:string= secondName.toLowerCase();
const firstUpperCase:string= firstName.toUpperCase();
const secondUpperCase:string= secondName.toUpperCase();
console.log("lowercase:")
console.log(firstLowerCase + " "+ secondLowerCase);
console.log("uppercase:")
console.log(firstUpperCase + " " + secondUpperCase);
console.log("TitalCase:")
console.log(firstName + " "+ secondName)
